<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body{
            font-family:Georgia, 'Times New Roman', Times, serif;
            font-size: 15px;
        }
        .home{
    position: absolute;
    left: 2px ;
    top: 3px;
    width: 20px;
    font-size: 25px;
    font-family: Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
}

.separate-blogs{
    display: flex;
    column-gap: 30px;
    margin : 30px 30px;
    flex-wrap: wrap;
    row-gap: 30px;
    justify-content: space-around;
}
.max{
    flex:1 1 11 1;
}

.box{
    border: 2px rgb(189, 179, 179) solid;
    display: flex;
    flex-direction: column;
    width: 300px;
    padding: 2px 2px;

}

img {
    height: 250px;
}
.title {
    border: 1px black solid;
    padding: 1px 1px;
    text-align: center;
    height: 30px;
    width: auto;
    align-content: center;
}
.button{
    background-color: rgb(98, 98, 215);
    color: rgb(255, 255, 254);
    text-align: center;
    height: 40px;
    font-size: 20px;
}
    </style>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Blogs</title>
</head>
<body>
    <% if (!locals.user) { %>
      <a href="/signin">Login</a> to see your blogs
    <% } else if (locals.indiBlogs && indiBlogs) { %>
    <a href="/" class="home">Home</a>
    <h1 class="mb-10"><center>Your blogs</center> </h1>
    <div class="separate-blogs">

       <% indiBlogs.forEach(blog => { %>
        <div class="max box">
            <img src="<%= blog.coverURL %>" width="290px">
            <h3 class="title"><%= blog.title %></h3>
            <a class=" button" href="/blog/view/<%= blog._id %>">View</a>
        </div>
       <% }) %>
    </div>
   <% } %>
    
</body>

<%- include('./partials/script') %>
</html>